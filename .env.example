# Production site URL. Drives canonical links, sitemap entries, OG URLs,
# robots.txt sitemap pointer, and every JSON-LD @id. Without this set,
# the site falls back to http://localhost:3000 and Google will not index it.
# Set this in Vercel's Environment Variables UI before the first deploy.
NEXT_PUBLIC_SITE_URL=https://watereject.dev
